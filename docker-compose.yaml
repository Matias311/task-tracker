version: "3.9"

services:

  postgres:
    image: postgres:16
    container_name: task-postgres
    env_file: .env
    ports:
      - "5432:5432"

  app:
    build: .
    container_name: task-app
    env_file: .env
    stdin_open: true
    tty: true
    depends_on:
      - postgres
